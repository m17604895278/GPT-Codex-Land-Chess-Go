military-chess-server/
├── go.mod
├── main.go

├── config/
│   └── config.go          # 基础配置（端口、房间上限等）

├── internal/
│   ├── room/
│   │   ├── room.go        # Room 结构体 & 房间管理
│   │   └── manager.go    # 创建 / 获取 / 删除房间

│   ├── board/
│   │   ├── board.go       # 棋盘结构
│   │   └── position.go    # 坐标、位置相关逻辑

│   ├── piece/
│   │   └── piece.go       # 棋子类型、等级、阵营

│   ├── rule/
│   │   └── rule.go        # 吃子、翻棋、胜负判定规则

│   ├── game/
│   │   ├── game.go        # 一局游戏的核心状态
│   │   ├── state.go       # 状态机（waiting / playing / finished）
│   │   └── action.go      # flip / move / surrender

│   ├── protocol/
│   │   ├── request.go     # 客户端请求结构
│   │   └── response.go    # 服务端响应结构

│   └── service/
│       └── game_service.go # 对外暴露的游戏服务接口

├── api/
│   └── handler.go         # HTTP / WebSocket 入口（后面再写）

└── README.md
